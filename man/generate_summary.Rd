% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_summary.R
\name{generate_summary}
\alias{generate_summary}
\title{Generate summary statistics for optimization solutions}
\usage{
generate_summary(
  data = NULL,
  out_dir = ".",
  run_id = "optimTFE",
  suitability_mx = NULL,
  spatial = NULL,
  spatial_crs = NULL,
  min_units_plot = file.path(out_dir, run_id, "n_units.pdf"),
  summary_out = file.path(out_dir, run_id, "summary.csv"),
  return_df = FALSE,
  progress = TRUE,
  cores = NULL,
  batch_size = NULL,
  max_batch_size = 1000
)
}
\arguments{
\item{data}{Dataframe containing solution data. If NULL, data will be loaded
from the specified output directory.}

\item{out_dir}{Character string specifying the output directory path. Default is ".".}

\item{run_id}{Character string specifying the run identifier. Default is "optimTFE".}

\item{suitability_mx}{Matrix of suitability values with unit IDs as row names and
species as column names.}

\item{spatial}{Spatial data frame (sf object) containing geometries for planning units.}

\item{spatial_crs}{CRS object for spatial data. By default this will be set to the CRS
of the spatial data using `sf::st_crs(spatial)`. However, calculations of spatial metrics
assume an equal area projection and a suitable crs can be provided to project the
existing spatial data. For exameple, `spatial_crs = sf::st_crs("+proj=utm +zone=4")`` will
project the existing spatial data to UTM zone 4 before calculating spatial metrics.}

\item{min_units_plot}{Character string specifying the output path for the minimum units
plot description (default = `file.path(out_dir, run_id, "n_units.pdf")`).}

\item{summary_out}{Character string specifying the output path for the summary
data frame (default = `file.path(out_dir, run_id, "summary.csv")`).}

\item{return_df}{Logical value indicating whether to return the summary data frame.}

\item{progress}{Logical value indicating whether to show progress bar}

\item{cores}{Number of cores to use for parallel processing}

\item{batch_size}{when parallel processing is used, this parameter can be
used to process solution in batches for improved efficiency (default =
NULL). If not provided (NULL), batch size will be calculated automatically.}

\item{max_batch_size}{limit batch size for parallel processing (default =
1000). Smaller values will allow the progress bar to update more
frequently, but at the cost of more disk writes.}
}
\value{
A data frame with summary statistics for each solution, including number
  of units, mean/median suitability, species richness metrics, and spatial metrics
  (area and perimeter) if spatial data is provided.
}
\description{
This function calculates various summary statistics for optimization solutions,
including unit counts, suitability metrics, species richness, and spatial
metrics (when spatial data is provided).
}
\seealso{
Other main: 
\code{\link{optimTFE}()}
}
\concept{main}
